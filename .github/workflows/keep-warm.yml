name: keep-warm

on:
  schedule:
    - cron: '*/3 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Neon
        run: curl -fsS --max-time 30 https://posmarket-eight.vercel.app/api/ping
